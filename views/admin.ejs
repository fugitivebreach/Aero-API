<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - AeroAPI</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <svg width="40" height="40" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <defs>
                        <linearGradient id="logoGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                            <stop offset="0%" style="stop-color:#00d4ff;stop-opacity:1" />
                            <stop offset="100%" style="stop-color:#00a8cc;stop-opacity:1" />
                        </linearGradient>
                    </defs>
                    <circle cx="20" cy="20" r="18" stroke="url(#logoGradient)" stroke-width="2" fill="none"/>
                    <path d="M20 8L30 28H10L20 8Z" fill="url(#logoGradient)"/>
                    <circle cx="20" cy="20" r="3" fill="#00d4ff"/>
                </svg>
                <span>AeroAPI</span>
            </div>
            <div class="nav-links">
                <a href="/dashboard" class="nav-link">Dashboard</a>
                <div class="user-info">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.discord_id %>/<%= user.discord_avatar %>.png" alt="Avatar" class="user-avatar">
                    <span><%= user.discord_username %></span>
                </div>
                <a href="/logout" class="btn-secondary">Logout</a>
            </div>
        </div>
    </nav>

    <div class="dashboard-container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <h3>Admin Panel</h3>
            </div>
            <nav class="sidebar-nav">
                <a href="#" class="sidebar-link active" data-tab="users">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                        <circle cx="9" cy="7" r="4"/>
                        <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
                        <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
                    </svg>
                    User Management
                </a>
            </nav>
        </aside>

        <main class="dashboard-main">
            <div id="users-tab" class="tab-content active">
                <div class="tab-header">
                    <h2>User Management</h2>
                    <div class="search-container">
                        <input type="text" id="user-search" placeholder="Search by username or ID..." class="search-input">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <circle cx="11" cy="11" r="8"/>
                            <path d="m21 21-4.35-4.35"/>
                        </svg>
                    </div>
                </div>

                <div id="users-container" class="users-grid">
                    <div class="loading-spinner">Loading users...</div>
                </div>
            </div>
        </main>
    </div>

    <div id="moderate-modal" class="modal">
        <div class="modal-content modal-large">
            <div class="modal-header">
                <h3 id="moderate-modal-title">Moderate User</h3>
                <button class="modal-close" onclick="closeModerationModal()">&times;</button>
            </div>
            <div class="modal-body">
                <div class="user-info-card" id="moderate-user-info"></div>
                
                <div class="form-group">
                    <label for="action-type">Action Type</label>
                    <select id="action-type" class="form-input" onchange="handleActionTypeChange()">
                        <option value="">Select Action</option>
                        <option value="lock">Lock Account</option>
                        <option value="disable">Disable Account</option>
                        <option value="ratelimit">Ratelimit Account</option>
                    </select>
                </div>

                <div id="ratelimit-duration" class="form-group" style="display: none;">
                    <label>Duration</label>
                    <div class="duration-inputs">
                        <input type="number" id="duration-days" placeholder="Days" class="form-input" min="0">
                        <input type="number" id="duration-hours" placeholder="Hours" class="form-input" min="0">
                        <input type="number" id="duration-seconds" placeholder="Seconds" class="form-input" min="0">
                    </div>
                </div>

                <div class="form-group">
                    <label for="moderation-reason">Reason *</label>
                    <textarea id="moderation-reason" class="form-textarea" placeholder="Enter reason for moderation..." rows="4"></textarea>
                </div>

                <div id="moderation-history" class="moderation-history"></div>
            </div>
            <div class="modal-footer">
                <button class="btn-secondary" onclick="closeModerationModal()">Cancel</button>
                <button class="btn-danger" onclick="confirmModeration()">Apply Moderation</button>
            </div>
        </div>
    </div>

    <div id="unmoderate-modal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3>Unmoderate User</h3>
                <button class="modal-close" onclick="closeUnmoderateModal()">&times;</button>
            </div>
            <div class="modal-body">
                <p>Select the moderation action to remove:</p>
                <div class="form-group">
                    <select id="unmoderate-action-type" class="form-input">
                        <option value="">Select Action</option>
                        <option value="lock">Remove Lock</option>
                        <option value="disable">Remove Disable</option>
                        <option value="ratelimit">Remove Ratelimit</option>
                    </select>
                </div>
            </div>
            <div class="modal-footer">
                <button class="btn-secondary" onclick="closeUnmoderateModal()">Cancel</button>
                <button class="btn-primary" onclick="confirmUnmoderation()">Unmoderate</button>
            </div>
        </div>
    </div>

    <script src="/js/admin.js"></script>
</body>
</html>
